import { NextConfig } from 'next';

const nextConfig: NextConfig = {
  // Enable static HTML export behavior for `next build`
  output: 'export',
  // keep other config as needed...
};

export default nextConfig;
